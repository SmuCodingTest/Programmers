function solution(array, commands) {
    var answer = [];

    commands.forEach((element)=>{
       let sliced = array.slice(element[0]-1, element[1]).sort((a, b) => a - b);
        answer.push(sliced[element[2]-1]);
    })

    return answer;
}